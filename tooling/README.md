# temp_test_scripts

⚠️ **Not part of the pipeline.** These are throwaway scripts I wrote for ad-hoc file transfer (a simple `file_client.py` / `file_server.py` pair) while moving files around during development. They have **no use in this project** and are unrelated to the synthetic data, YOLO, or GDRNPP workflow.

I don't know why they're still here jk, I didn't cleaned them up, so they were coming in quite a few initial comits so I let them be. Safe to ignore — and safe to delete.

## Contents

- `file_client.py` — ad-hoc file transfer client
- `file_server.py` — ad-hoc file transfer server
- `file_server.log` — leftover log from a test run
- `FILE_SERVER_SETUP.md` — notes for running the above
